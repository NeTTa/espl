#include "../include/complines.h"
#include <stdio.h>
#include <string.h>

void complines(*char A, *char B)
{
    char lineA[80];
    char lineB[80];
    FILE *readA = fopen("a.txt","r");
    FILE *readB = fopen("a.txt","r");
    
    while(fgets(line,1,sizeof(line),fp)
    {
    // do something
    }
    fclose(reafA);
    fclose(reafB);
}